import javax.swing.*;
import java.awt.*;
import java.awt.event.*;

public class NumberOfGuesses extends JFrame {
    public NumberOfGuesses(String t) {
        setTitle(t);
        setSize(300, 300);
        setLocation(200, 150);
        setVisible(true);
    }
}
